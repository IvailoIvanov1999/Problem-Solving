numbers = [int(x) for x in input().split(', ')]

for el in range(len(numbers)):
    el = str(el)
    for lenght_word in range(len(el)):
        pass
for i in numbers:
    i = str(i)
    if i[0] == i[-1]:
        print('True')
    else:
        print('False')


